// Получение содержимого таблицы UserTypes
const char* select_from_user_types = "SELECT * FROM UserTypes";

// Получение содержимого таблицы Users
const char* select_from_users = "SELECT * FROM Users";

// Получение содержимого таблицы Security
const char* select_from_security = "SELECT * FROM Security";

// Получение содержимого таблицы TaskStatuses
const char* select_from_task_statuses = "SELECT * FROM TaskStatuses";

// Получение содержимого таблицы Tasks
const char* select_from_tasks = "SELECT * FROM Tasks";

// Получение содержимого столбца user_unique_login из таблицы Users
const char* select_unique_login = "SELECT user_unique_id, user_unique_login FROM Users";


